language: node_js

services: docker

node_js:
    - 14

before_script:
    - git clone https://github.com/DanielRuizMed/PAndemium ./app

script: cd app && docker run -t -v `pwd`:/test danielruizmed/pandemium ls -l