language: node_js

services: docker

node_js:
    - 14

build:
    pre-ci:
        - git clone https://github.com/DanielRuizMed/PAndemium ./app

    ci:
        - cd app && docker run -t -v `pwd`:/test danielruizmed/pandemium ls -l