language: node_js

services: docker

node_js:
    - 14

build:
    ci:
        - git clone https://github.com/DanielRuizMed/PAndemium ./app
        - cd app && docker run -t -v `pwd`:/test danielruizmed/pandemium